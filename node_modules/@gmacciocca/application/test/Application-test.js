import Application from "../src/Application";
// import sinon from "sinon";
// import { expect } from "chai";

describe("Application", function() {
    beforeEach(() => {
        this.app = Application.create();
        // this.cb = sinon.stub();
        // this.fn = preventDefault(this.cb);
    });
    it("Implements interface", () => {
        //it("Implements bootstrap", () => true);
        // it("Implements bootstrap", () => this.app.should.respondTo("bootstrap"));
        // it("Implements shutdown", () => this.app.should.respondTo("shutdown"));
        // it("Implements destroy", () => this.app.should.respondTo("destroy"));
    });
});
