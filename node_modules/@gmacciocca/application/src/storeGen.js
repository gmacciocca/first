// import { DependencyClass } from "@gmacciocca/dependencies";
// import { Storage } from "../../Storage";
//
// const storeGen = (schemas, dependencies) => {
//     Object.keys(schemas || {}).forEach(storeType => {
//         dependencies.add(new DependencyClass(`storage${storeType}`, Storage, storeType, schemas[storeType]));
//     });
// };
//
// export default storeGen;
