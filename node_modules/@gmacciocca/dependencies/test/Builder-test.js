import Builder from "../src/Builder";
// import sinon from "sinon";
// import { expect } from "chai";

describe("Builder", function() {
    beforeEach(() => {
        this.builder = new Builder();
        // this.cb = sinon.stub();
        // this.fn = preventDefault(this.cb);
    });
    it("Implements interface", () => {
        it("Implements add", () => this.builder.should.respondTo("add"));
        it("Implements build", () => this.builder.should.respondTo("build"));
        it("Implements callMethodOnDependencies", () => this.builder.should.respondTo("callMethodOnDependencies"));
    });
});
